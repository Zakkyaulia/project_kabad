<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Project Kabad</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;700&display=swap" rel="stylesheet">
    <style>body { font-family: 'Plus Jakarta Sans', sans-serif; }</style>
</head>
<body class="bg-slate-50 min-h-screen">
    <nav class="bg-white/80 backdrop-blur-md border-b border-slate-200 sticky top-0 z-50">
        <div class="container mx-auto px-6 py-4 flex justify-between items-center">
            <div class="flex items-center space-x-3">
                <div class="w-10 h-10 bg-green-600 rounded-xl flex items-center justify-center shadow-lg shadow-green-200">
                    <span class="text-white font-bold text-xl">K</span>
                </div>
                <h1 class="text-xl font-bold text-slate-800">Kabad <span class="text-green-600">Pro</span></h1>
            </div>
            <div class="flex items-center space-x-6">
                <span id="userGreeting" class="hidden md:inline text-slate-600 font-medium"></span>
                <button onclick="logout()" class="bg-red-50 text-red-600 px-5 py-2.5 rounded-xl font-bold hover:bg-red-100 transition-all border border-red-100">
                    Keluar
                </button>
            </div>
        </div>
    </nav>

    <main class="container mx-auto px-6 py-10">
        <div class="flex flex-col md:flex-row md:items-center justify-between gap-6 mb-12">
            <div>
                <h2 class="text-3xl font-extrabold text-slate-800 tracking-tight">Dashboard Saya</h2>
                <p class="text-slate-500 mt-1">Kelola semua dokumentasi kegiatan Anda dengan mudah.</p>
            </div>
            <button class="bg-green-600 text-white px-6 py-3.5 rounded-2xl font-bold hover:bg-green-700 transition-all shadow-lg shadow-green-100 flex items-center gap-2">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M10 3a1 1 0 011 1v5h5a1 1 0 110 2h-5v5a1 1 0 11-2 0v-5H4a1 1 0 110-2h5V4a1 1 0 011-1z" clip-rule="evenodd" />
                </svg>
                Tambah Bukti Baru
            </button>
        </div>

        <div class="bg-white p-8 rounded-3xl shadow-sm border border-slate-200 mb-10 flex items-center gap-6">
            <div class="bg-green-50 p-4 rounded-2xl">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-green-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
            </div>
            <div>
                <p class="text-sm font-bold text-slate-400 uppercase tracking-widest">Total Bukti Aktif</p>
                <p id="totalCount" class="text-4xl font-black text-slate-800 mt-1">0</p>
            </div>
        </div>

        <div class="bg-white rounded-3xl shadow-sm border border-slate-200 overflow-hidden">
            <div class="overflow-x-auto">
                <table class="w-full text-left">
                    <thead class="bg-slate-50/50">
                        <tr>
                            <th class="px-8 py-5 text-sm font-bold text-slate-500 uppercase tracking-wider">Keterangan Aktivitas</th>
                            <th class="px-8 py-5 text-sm font-bold text-slate-500 uppercase tracking-wider text-right">Aksi</th>
                        </tr>
                    </thead>
                    <tbody id="keaktifanBody" class="divide-y divide-slate-100">
                        </tbody>
                </table>
            </div>
        </div>
    </main>

    <script>
        const token = localStorage.getItem('token');
        if (!token) window.location.href = '/login';

        async function loadData() {
            const response = await fetch('/api/keaktifan', {
                headers: { 'Authorization': `Bearer ${token}` }
            });
            const data = await response.json();
            const body = document.getElementById('keaktifanBody');
            const totalCount = document.getElementById('totalCount');
            
            body.innerHTML = '';
            totalCount.innerText = data.length;

            data.forEach(item => {
                body.innerHTML += `
                    <tr class="hover:bg-slate-50/50 transition-colors">
                        <td class="px-8 py-5">
                            <span class="text-slate-700 font-semibold">${item.keterangan}</span>
                        </td>
                        <td class="px-8 py-5 text-right">
                            <button onclick="deleteData(${item.id})" class="text-red-500 hover:text-red-700 font-bold px-4 py-2 rounded-lg hover:bg-red-50 transition-all">
                                Hapus
                            </button>
                        </td>
                    </tr>
                `;
            });
        }

        async function deleteData(id) {
            if (confirm('Yakin ingin menghapus data ini?')) {
                await fetch(`/api/keaktifan/${id}`, {
                    method: 'DELETE',
                    headers: { 'Authorization': `Bearer ${token}` }
                });
                loadData();
            }
        }

        function logout() {
            localStorage.removeItem('token');
            window.location.href = '/login';
        }

        loadData();
    </script>
</body>
</html>